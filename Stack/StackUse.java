package Stack;



public class StackUse {
   /* public static void main(String[] args) {
        int n=5;
        StackUsingArray stack=new StackUsingArray(5);
        int i=0;
        while(stack.isEmpty())
        { try {
            stack.push(i);
            i++;
            }
            catch (StackFullException e)
            {
                System.out.println("The Stack is full You cannot enter more elements in the stack");

            }

        }
        while(!stack.isEmpty())
        {   try {
            System.out.println(stack.pop());
                }
                catch(StackEmptyException e)
            {
                System.out.println("The Stack is empty ,Can't pop more .");
            }

        }

    }
    */
}
